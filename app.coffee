# Refactored to Coffee Script, 2014

getTabs = ->
    chrome.tabs.getAllInWindow(tabList) {
        window.tabs = tabList
        tabList.score
        tabsData = tabsList
    }

query = (input) ->
    
